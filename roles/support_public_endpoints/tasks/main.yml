- name: Create Caddyfile in /etc/Caddy/ for RPC and API
  become: true
  template:
    src: 'Caddyfile'
    dest: /etc/caddy/{{network}}.caddy
    owner: 'root'
    group: 'root'
    mode: '0644'

- name: restart caddy service
  become: true
  systemd:
    name: caddy
    state: restarted
    daemon_reload: yes
    enabled: yes
  changed_when: false

- name: open gPRC port
  become: true
  ufw:
    rule: allow
    port: '{{ custom_port_prefix }}90'
    proto: tcp
    