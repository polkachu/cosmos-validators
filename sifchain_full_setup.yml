---
# - name: Prepare
#   hosts: '{{ target }}'
#   become: true
#   gather_facts: false
#   roles:
#     - prepare

- name: Full setup
  hosts: '{{ target }}'
  gather_facts: false
  roles:
    - sifchain
